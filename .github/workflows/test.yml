name: TEST

on: ['push']

jobs:

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:

      - uses: actions/checkout@master
      -
        name: output
        id: output
        run: |
          echo ${{ github.event.commits[0].message }}
